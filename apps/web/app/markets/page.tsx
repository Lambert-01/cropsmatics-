import { ComingSoon } from "@/components/layout/ComingSoon";

export const metadata = { title: "Markets — Cropmatics Rwanda" };

export default function MarketsPage() {
  return (
    <ComingSoon
      title="Markets"
      description="Market prices and aggregation economics require a licensed price feed. No price data is shown until a verified, attributable source is registered."
    />
  );
}
